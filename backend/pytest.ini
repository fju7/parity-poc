[pytest]
markers =
    eval: live model-drift eval that calls the deployed API (on-demand; excluded from default/CI runs)
# Default/CI runs skip live-eval tests; run them explicitly with `-m eval`.
addopts = -m "not eval"
